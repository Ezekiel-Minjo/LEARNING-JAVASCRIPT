// allows automate repetitive tasks
// for loop keeps running while condition is TRUE
for(let rep = 1; rep <= 10; rep++) {
    console.log(`Lifting weights repetitio ${rep}`);
}

// loop inside a loop
